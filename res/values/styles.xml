<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomPopupMenu" parent="ThemeOverlay.AppCompat.Light">
        <!-- Background color for the PopupMenu -->
        <item name="android:popupBackground">@color/subtleBlue1</item>
        <!-- Text color for the options -->
        <item name="android:textColor">@color/white</item>
        <item name="android:itemBackground">@drawable/custom_menu_item_background</item>
    </style>

    <style name="ToolbarTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
